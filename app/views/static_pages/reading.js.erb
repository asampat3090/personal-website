<% if @articles.all.count>0 -%>
$('#articles').append('<%= escape_javascript(render(partial: 'articles')) %>');
$('a.hook').attr('href', '<%= reading_path(page: (params[:page].to_i || 1)+1, per_page: params[:per_page] || 5) %>').show();
<% else -%>
$('a.hook').hide();
<% end -%>